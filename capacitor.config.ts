import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.bcss.fhem',
  appName: 'FhemNative',
  webDir: 'projects/fhem-native-mobile/src',
  bundledWebRuntime: false
};

export default config;
